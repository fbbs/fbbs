#ifndef H_TYPES
#define H_TYPES

typedef unsigned int UINT;

typedef void * PVOID;

typedef long LONG;
typedef unsigned long ULONG;
typedef unsigned long DWORD;

typedef long LRESULT;
typedef unsigned char BYTE;
typedef BYTE * PBYTE;

typedef enum{false=0,true} bool;	//”ÎANSI 99≥ÂÕª?

/*
#define SWITCH(x) {char *p=x;
#define CASE(x)   if(!strcmp(p,x)){
#define BREAK     } else
#define DEFAULT   {
#define END_SWITCH  }}
*/

#endif
