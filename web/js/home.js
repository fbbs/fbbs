(function ($, f) {
    'use strict';

    var page = {
        components: [],
        context: {},
        methods: {

        },
        beforeInit: function () {
            ;
        },
        oninit: function () {
            this._bindEvents();
        },
        _bindEvents: function () {
            ;
        }
    };

    f.bbsPage = new f.page(page);
}(jQuery, f));
