(function ($, f) {
    'use strict';

    var page = {
        components: ['Login'],
        context: {},
        methods: {

        },
        beforeInit: function () {
            ;
        },
        oninit: function () {
            this._bindEvents();
        },
        _bindEvents: function () {
            ;
        }
    };

    f.bbsPage = new f.page(page);
}(jQuery, f));
